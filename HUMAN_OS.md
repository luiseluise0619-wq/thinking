# HUMAN OS — 북극성 비전

> THINK OS의 궁극 포지션. AI 비서가 아니라, **사용자가 스스로 성장하는 인간이 되도록 돕는 성장 운영체제**.
> 목표는 답을 주는 것이 아니라 — 더 깊이 생각하고, 더 빠르게 배우고, 더 좋은 판단을 하고, 더 많이 실행하게 만드는 것.

이 문서는 **비전**이다. 구현 원칙은 하나: *기능을 늘리지 말고 하나의 성장 루프를 압도적으로 잘 만든다.*
아래 7개 영역을 전부 별도 모듈로 짓는 것은 명시적으로 **비권장**한다. 대부분은 이미 핵심 루프에서 측정되고 있고,
남은 것은 로드맵으로 둔다.

---

## 성장 루프 (핵심)

```
경험 → 기억 → 학습 → 이해 → 사고 → 창의 → 판단 → 실행 → 피드백 → 지혜 → 더 나은 경험
```

THINK OS가 이미 구현한 닫힌 루프가 이 큰 루프의 **작동 엔진**이다:
```
문제 정의 → 생각 기록 → 숨은 가정·반증 → 본질 → 사고 프레임 → 실행 약속 → 결과 되먹임 → 원칙 축적 → Genome
```

## 코치 행동 규칙 (앱의 AI 시스템 프롬프트에 반영됨)

| 절대 안 함 | 항상 함 |
|---|---|
| ❌ 대신 생각하기 | ⭕ 좋은 질문 만들기 |
| ❌ 즉답·해결책 주기 | ⭕ 사고의 허점 찾기 |
| ❌ 선택지 남발 | ⭕ 경험을 지식으로 변환 |
| ❌ 칭찬으로 끝내기 | ⭕ 행동으로 연결 · 성장 패턴 추적 |

→ 공통 정체성 `HUMAN_OS_ROLE`로 모든 AI 질문(반박·논쟁·코칭)에 주입, 백엔드 Critic Agent에도 반영.

## 8단계 사고 진행 방식 → 현재 구현 매핑

| HUMAN OS 단계 | THINK OS 구현 |
|---|---|
| 1. 문제 정의 | 루틴 STEP 1 · Problem Agent |
| 2. 현재 생각 기록 | 루틴 STEP 2 (강제 기록) |
| 3. 숨은 가정 찾기 | 적응형 Critic (assert 상태) |
| 4. 반증 조건 | Critic 첫 질문 = "틀렸다면 어떤 증거가?" |
| 5. 본질까지 | 1원칙 프레임 · 본질 도달률 지표 |
| 6. 패턴·원리 | 멘탈 모델 · 사고방식 빈도 |
| 7. 실행 연결 | 실행 약속 (⑩) |
| 8~9. 결과 기록 → 원칙 | 피드백 루프 · Wisdom Log |

---

## 7대 성장 영역 — 무엇이 측정되고, 무엇이 로드맵인가

| # | 영역 | 현재 측정/구현 | 상태 |
|---|---|---|---|
| 1 | 인지력 (Thinking) | 본질 도달률·논리적 비약·사고 깊이·반례 고려율 · 사고 프레임 10종 | ✅ 구현 |
| 2 | 학습력 (Learning) | 적응형 코치의 learn 모드(구조·비유·파인만) · 학습력 능력치 | ✅ 부분 |
| 3 | 기억력 (Memory) | Wisdom Log(원칙 진화) · 지식 노드 스텁 · 원칙 재사용률 | 🟡 부분 (Vector DB는 로드맵) |
| 4 | 창의력 (Creation) | 역발상 프레임 · 창의력 능력치 | 🟡 부분 |
| 5 | 판단력 (Decision) | 적응형 코치의 decide 모드(가역성·비대칭) · 확률 사고 프레임 | ✅ 구현 |
| 6 | 실행력 (Execution) | 실행 약속 → 결과 → 실행 완료율(실측) | ✅ 구현 |
| 7 | 사회 지능 (Social) | — | 🔴 로드맵 (유일하게 새 영역) |

**결론:** 7개 중 6개는 이미 핵심 루프 안에서 측정된다. 지금 필요한 것은 새 모듈이 아니라
**이 루프를 10명이 30일 동안 실제로 쓰게 만들고, 사고 방식이 변했는지 검증**하는 것이다.
(사회 지능은 별도 영역이라 스코프가 크게 다르며, 코어 검증 이후로 미룬다.)

## 검증 지표 (성장 데이터 = 진짜 해자)

시간이 지나면 개인 프로필이 답한다: *"나는 어떤 상황에서 강한가 / 어떤 판단 오류를 반복하는가 / 어떻게 성장하는가."*
현재 Genome이 이미 측정하는 축 — 사고 깊이 추이, 본질 도달률 추이, 실행 완료율, 원칙 재사용률 —
이 **30일 검증의 계기판**이다. `GET /dna/{user_id}`가 서버측 리포트를 제공한다.

## 최종 목표

사용자가 AI에 **의존하는** 사람이 되는 것이 아니라, AI를 활용해 **스스로 성장하는** 인간이 되게 하는 것.
